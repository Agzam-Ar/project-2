

const Vars = {
	
	env: {
		url: 'https://agzam-ar.github.io/project-2'
		//process.env.NODE_ENV == 'development' ? "http://localhost:3000/" : (process.env.URL == undefined ? 'https://agzam-ar.github.io/project-2' : process.env.URL),
	// process.env.NODE_ENV == 'development' ? "http://localhost:3000/" : 
	}
};


export default Vars;